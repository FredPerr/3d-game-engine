package engine.event.event.render;

/**
 * Created by KitK4t on 2018-12-27.
 */
public enum CameraSettings {

    FOV,
    RENDER_DISTANCE_FAR,
    RENDER_DISTANCE_NEAR,
    LOCATION,
    ROTATION,
    SENSITIVITY,
    //cap the pitch in min and max.
    LOCK_PITCH,
    PITCH_MIN,
    PITCH_MAX
}
